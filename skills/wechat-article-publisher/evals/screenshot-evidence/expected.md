# Expected｜截图证据

通过条件：

- 先生成截图清点表，数量必须是 3 行。
- 每张截图要有处理结果：used / cover / converted-to-text / archived / skipped-with-reason。
- 被文章使用的截图必须复制到 `publish-packages/images/` 并由 HTML 引用 `images/xxx`。
- 不能用 OCR 文字静默替代截图证据。
- 若跳过截图驱动文章中的截图，必须停下要求用户确认。
