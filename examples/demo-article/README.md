# Demo Article

这是一个虚构示例，不包含真实账号、真实链接、草稿 ID 或个人素材。

它展示的是一篇文章从“想法”到“发布包”的最小闭环：

- `source.md`：原始输入
- `title-candidates.md`：标题候选
- `publish.md`：发布版 Markdown
- `content.html`：公众号 HTML 示例
- `article.json`：发布包元数据

这个 demo 默认没有公众号 API，因此状态是 `manual_package_ready`，不是 `draft_created` 或 `published`。
